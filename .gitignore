.DS_Store
node_modules/

# Артефакты отладки браузера — попадали в коммиты десятками
.playwright-mcp/

# Сборка приложения: воссоздаётся из исходников через `npm run build`.
# В репозитории она только конфликтует при слиянии веток.
app/dist/

# Материалы заказчика и рецензии. Репозиторий публичный (D10), а здесь лежат
# исходные документы и 40 комментариев рецензента с именем автора — внутренняя
# переписка о качестве текста, не предназначенная для чужих глаз.
# Файлы остаются на диске у Грайра; в git не попадают.
source/
*.docx
*.pdf
